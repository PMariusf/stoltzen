import ContentPage from "@/components/layout/ContentPage";

export default function ResultsPage() {
  return (
    <ContentPage
      locale="en"
      eyebrow="Start & results"
      title="Find runner and time"
      description="The next step is a searchable results system with name, bib number, class, club, year, time, placing and personal history."
    />
  );
}
