import ContentPage from "@/components/layout/ContentPage";

export default function PhotosPage() {
  return (
    <ContentPage
      locale="en"
      eyebrow="Photos"
      title="Stoltzen in photos"
      description="Year-by-year galleries, search by bib number or name and a personal Stoltzen photo experience will be built here when the photo archive is connected."
    />
  );
}
